/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: { //Ativar a compilação dos componentes styled no console
    styledComponents: true
  }
}

module.exports = nextConfig
